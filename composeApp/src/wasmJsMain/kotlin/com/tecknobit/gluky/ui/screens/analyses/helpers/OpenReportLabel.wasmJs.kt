package com.tecknobit.gluky.ui.screens.analyses.helpers

import org.jetbrains.compose.resources.StringResource

/**
 * `openReportLabel` the label text to display on a snackbar message to allow to open directly the report created,
 * on the web target is null due, at the moment, of the impossibility to open a file from the browser
 */
actual val openReportLabel: StringResource? = null